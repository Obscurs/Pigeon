Task: Fix 2 blocking issues from ARTEM review
Iteration: 2

Changed files:
- Code/PigeonLib/Pigeon/UI/UIEventSystem.cpp — added `const` to the view<> template argument for pig::InputStateSingletonComponent (line 47), matching the read-only declaration in readSet
- .claude/docs/coding-guidelines.md — replaced the stale rule "A type must not appear in both writeSet and addSet on the same system" with the corrected statement that a type MAY appear in both, explaining the deferred-add + immediate-modify use case

Implementation notes:
The viewInput call was the only call site using a mutable view for a read-only component in this file. The line immediately below it already used get<const pig::InputStateSingletonComponent> for the actual retrieval, so the only change needed was the view<> template argument itself. The coding-guidelines edit is a pure documentation correction with no code impact.

Known edge cases / open questions:
none
