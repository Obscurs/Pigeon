Task: Complete test coverage for every concrete system; rename all legacy test files
Iteration: 1
Verdict: APPROVED

--- SAB (application/engine) ---
No source changes in this task. SAB review not applicable.

--- DYM (tests) ---
APPROVED with suggestions. No blocking issues.

[SUGGESTION] Code/UT/UT_Core/ConfigLoaderSystemTest.cpp:1, Code/UT/UT_Core/ResourceManagerSystemTest.cpp:1, Code/UT/UT_Renderer/Renderer2DSystemTest.cpp:1, Code/UT/UT_Sandbox/ConfigLoaderSystemTest.cpp:1, Code/UT/UT_Sandbox/SampleUISystemTest.cpp:1 — All five new test files open with `#pragma once`. This directive has no effect in a .cpp translation unit. This was raised in the previous iteration and remains unfixed across all five new files.

[SUGGESTION] Code/UT/UT_Renderer/Renderer2DSystemTest.cpp — pig::Renderer2DSystem is an engine system (PigeonLib), not a SandboxApp module system. The testing guidelines list "renderer" explicitly under UT_Core as an engine primitive folder. Placing the file in a new UT_Renderer folder is structurally consistent with how SandboxApp modules are organised, but deviates from the guidelines which group all PigeonLib engine systems under UT_Core. Consider moving to UT_Core if strict guideline alignment is required.

[SUGGESTION] Code/UT/UT_Core/WorldTest.cpp:163,176,199,221 — The renamed legacy file uses test name formats that do not follow the required `"<ModuleName>.<SystemName>::<Description>"` pattern. Examples: "Core.ECS::World" (SystemName missing), "Core.ECS.SystemOrdering::WriterRunsBeforeReader" (extra dot segment). World is infrastructure not a system, so exact conformance is not enforceable here, but the inconsistency is visible.

Summary:
All five new system test files are structurally correct: one file per system, correct folder placement (with the UT_Renderer note above), isolated world per test case, state seeded via GetRegistryDirect(), only the system under test is registered, CMakeLists.txt correctly adds UT_Renderer and UT_Sandbox globs and compiles the two SandboxApp system sources. Coverage is adequate across happy paths, guard conditions, and DeclareAccess verification for all five systems. The four renamed files preserve their original test content without loss. The deleted UT_UISystemTest.cpp was confirmed empty. No blocking issues exist.
