Task: Complete test coverage for every concrete system; rename all legacy test files
Iteration: 1

Test files:
- UT/UT_Core/ConfigLoaderSystemTest.cpp    — pig::ConfigLoaderSystem
- UT/UT_Core/ResourceManagerSystemTest.cpp — pig::ResourceManagerSystem
- UT/UT_Renderer/Renderer2DSystemTest.cpp  — pig::Renderer2DSystem
- UT/UT_Sandbox/ConfigLoaderSystemTest.cpp — sbx::ConfigLoaderSystem
- UT/UT_Sandbox/SampleUISystemTest.cpp     — sbx::SampleUISystem

Renamed (legacy UT_ prefix removed):
- UT_Core/UT_ECSTest.cpp                  -> UT_Core/WorldTest.cpp
- UT_Core/UT_CameraTest.cpp               -> UT_Core/OrthographicCameraTest.cpp
- UT_Core/UT_UUIDTest.cpp                 -> UT_Core/UUIDTest.cpp
- UT_Interface/UT_Dx11RendererAPITest.cpp -> UT_Interface/Dx11RendererAPITest.cpp

Deleted:
- UT_UI/UT_UISystemTest.cpp  (intentionally empty; content already migrated to per-system files)

CMakeLists.txt changes:
- Added file(GLOB SRC_TESTS_RENDERER "UT_Renderer/*.cpp")
- Added file(GLOB SRC_TESTS_SANDBOX "UT_Sandbox/*.cpp")
- Added SRC_SANDBOX_SYSTEMS with ConfigLoaderSystem.cpp and SampleUISystem.cpp
- Added source_group entries for Tests/Renderer and Tests/Sandbox
- Added "${PROJECT_SOURCE_DIR}/Code/SandboxApp" to include_directories
- Added SRC_TESTS_RENDERER, SRC_TESTS_SANDBOX, SRC_SANDBOX_SYSTEMS to add_executable

Test results: PASS
All 223 assertions in 78 test cases passed.

Expected behaviors (for SAB):
- pig::ConfigLoaderSystem: On first Update() with no EngineConfigSingletonComponent, creates
  one via deferred add (visible next frame). Subsequent updates do nothing (guard: no
  duplicate). Loaded config has non-null UUID fields for quad shader, text shader, and font.
- pig::ResourceManagerSystem: On first Update() with no ResourceMapSingletonComponent,
  creates one via deferred add (visible next frame). Subsequent updates do nothing (guard:
  no duplicate). Loaded map always has a default texture entry and at least one shader.
- pig::Renderer2DSystem: Returns early (no crash, no RendererDataSingletonComponent created)
  if any of camera, resource map, or engine config are absent. DeclareAccess correctly
  declares all read/add/write component types.
- sbx::ConfigLoaderSystem: On first Update() with no SampleUIConfigSingletonComponent,
  creates one via deferred add. Subsequent updates do nothing. Loaded config has non-null
  UUIDs for ui1, ui2, default font, and main layout.
- sbx::SampleUISystem: Returns early with no SampleUISingletonComponent created if either
  ResourceMapSingletonComponent or SampleUIConfigSingletonComponent is absent. On first
  valid Update(), creates SampleUISingletonComponent and OrthographicCameraComponent via
  deferred add. Created component carries FontID copied from config. DeclareAccess correctly
  covers all write/add/inframeAdd/read component types.

Bugs found:
- pig::ResourceManagerSystem: Log messages show "Failed to load font" for
  Assets/UT/Fonts/opensans/OpenSans-Bold.ttf and Assets/UT/Fonts/opensans/OpenSans-Regular.ttf.
  These fonts are referenced in Assets/UT/ResourcesManifest.json but the files do not exist
  under Data/Assets/UT/Fonts/. This is a data/asset gap, not a code bug; the system continues
  gracefully and tests still pass.
