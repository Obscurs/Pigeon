Task: Rewrite and expand the test suite to cover every registered system (PLAN.md step 1 — full coverage pass)
Iteration: 2

Test files:
- Code/UT/UT_UI/UIControlSystemTest.cpp  — UIControlSystem (7 test cases)
- Code/UT/UT_UI/UIEventSystemTest.cpp    — UIEventSystem   (8 test cases)
- Code/UT/UT_UI/UIRenderSystemTest.cpp   — UIRenderSystem  (4 test cases)
- Code/UT/UT_Core/InputSystemTest.cpp    — InputSystem     (9 test cases)
- Code/UT/UT_Core/CameraSystemTest.cpp   — CameraSystem    (8 test cases)

Pre-existing files that were broken by the ECS migration and were fixed:
- Code/UT/UT_Core/UT_CameraTest.cpp            — removed deleted #include <Pigeon/Renderer/Renderer2D.h>
- Code/UT/UT_Core/UT_Renderer2DTest.cpp        — cleared; old Renderer2D static API no longer exists
- Code/UT/UT_Interface/UT_Dx11RendererAPITest.cpp — removed deleted #include <Pigeon/Renderer/Renderer2D.h>
- Code/UT/UT_UI/UT_UISystemTest.cpp            — cleared; all tests referenced deleted types
  (pig::ui::RendererConfig, IUIControlSystemHelper, IUIRenderSystemHelper, m_LayoutFilePath,
   UIUpdateImageUUIDOneFrameComponent::m_PreviousImageToDestroy — none exist in current code)

Test results: PASS
All tests passed (163 assertions in 56 test cases)

The 4 "Assertion Failed" log lines in the output are expected — they come from
UT_UUIDTest.cpp's "Fail invalid strings" section which intentionally provokes UUID parse errors.

Expected behaviors (for SAB): N/A — all tests pass, no implementation changes needed.

Bugs found:
- Pre-existing (not new bugs): UT_UISystemTest.cpp referenced pig::ui::RendererConfig (old type,
  now RendererConfigSingletonComponent), IUIControlSystemHelper, IUIRenderSystemHelper, helper
  constructor arguments on UIRenderSystem/UIControlSystem, and
  UIUpdateImageUUIDOneFrameComponent::m_PreviousImageToDestroy. None of these exist.
- Pre-existing: UT_CameraTest.cpp, UT_Renderer2DTest.cpp, UT_Dx11RendererAPITest.cpp
  included deleted header Pigeon/Renderer/Renderer2D.h (Renderer2D was converted to ECS system).
